services:
  - type: web
    name: lawfirm-assistant
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: cd webapp && uvicorn main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
